apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    stack: edegal
    component: rabbitmq
  name: rabbitmq-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1000Mi
