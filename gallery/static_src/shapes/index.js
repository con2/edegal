import {PropTypes} from 'react';


export const PictureShape = PropTypes.shape({
  path: PropTypes.string.isRequired,
});

export const SubAlbumShape = PropTypes.shape({
  path: PropTypes.string.isRequired,
});
